apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "hello-aws.fullname" . }}-test-connection"
  labels:
    {{- include "hello-aws.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "hello-aws.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
