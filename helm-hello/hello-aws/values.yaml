replicaCount: 2

image:
  repository: hello-aws
  tag: "v1"
  pullPolicy: Never

service:
  type: ClusterIP
  port: 80

resources:
  requests:
    cpu: "50m"
    memory: "32Mi"
  limits:
    cpu: "100m"
    memory: "64Mi"

namespace: default
